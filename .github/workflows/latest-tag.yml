name: Update "latest" tag
on:
  push:
    tags: [v*.*.*]
jobs:
  add-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          git tag -d latest || true
          git tag latest
          git push --tags
